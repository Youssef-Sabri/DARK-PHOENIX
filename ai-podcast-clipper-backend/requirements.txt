# setuptools<81 / wheel are pinned here so they're present in the runtime env.
# The same pin is ALSO enforced at wheel-BUILD time via PIP_CONSTRAINT in
# main.py, because pip builds in isolated envs that a plain requirement line
# can't reach. setuptools>=81 removed pkg_resources, which whisperx@v3.2.0 and
# transitive deps need. See m-bain/whisperX#1210.
setuptools<81
wheel
tqdm
torch==2.0.1
torchaudio==2.0.2
torchvision==0.15.2
opencv-python
ffmpegcv
numpy<2.0
python_speech_features
scipy
# Pin to the last 0.5.x release: PySceneDetect >=0.6 removed the
# `scenedetect.video_manager` module / `VideoManager` class, but the vendored
# asd/demoTalkNet.py imports `from scenedetect.video_manager import VideoManager`
# (the 0.5.x API). Without this pin demoTalkNet.py dies on import with
# `ModuleNotFoundError: No module named 'scenedetect.video_manager'`, so no
# tracks/scores are produced and process_clip raises FileNotFoundError -> the
# backend 500s and 0 clips are created.
scenedetect==0.5.6.1
scikit-learn
gdown
pandas
transformers
accelerate
datasets
google-genai
pysubs2
boto3
fastapi[standard]
whisperx @ git+https://github.com/m-bain/whisperx.git@v3.2.0
pyannote-audio==3.1.1
